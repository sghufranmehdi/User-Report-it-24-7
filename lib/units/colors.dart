import 'package:flutter/material.dart';

// Global
const bgColor = Colors.white;

// Main theme
const alphaColor = Color(0xff3ea339);
const betaColor = Color(0xff9f7d24);

// Splash Screen
const gammaColor = Colors.white;

// Fields color
const inputFieldColor = Color(0xffffffff);

// Onboarding screen
const inactiveDotsColors = Color(0xFF0a0800);

// Other Constant
const tangoTextColor = Color(0xff9f7d24);
const gridShadowColor = Color(0xFF0f540d);
